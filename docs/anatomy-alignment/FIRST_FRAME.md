# 새 해부 장면의 첫 프레임 설정 — 2026-09-21

## 직접 재현한 결함

렌더러 호출 직전의 가시 메쉬와 재질, 호출 후 그리기 통계를 계측했습니다. 원본 좌표·카메라·재질을 계측기가 변경하지 않습니다. 기존 `dc39072`에서 다음 두 전환의 미설정 프레임을 확인했습니다.

| 요구 | 수정 전 관찰 | 수정 후 같은 계측 |
| --- | --- | --- |
| 0.5% 박리 입력에 맞춰 새 골격 표시 | 남성34.5%에서 새 골격278개의 불투명도가 첫 프레임1.0, 다음 프레임0.05. 전체 그리기 호출677→955 | 새 골격이 처음부터0.05. 미설정 불투명 프레임 없음 |
| 독립 CT→여성 전신 복귀에서 요청 계통만 표시 | 한 반복에서 여성1,220개가 모두 가시 상태·불투명도1.0으로 전달된 뒤 장기293개로 제한됨. 전자 프레임의 실제 그리기 호출627 | 처음부터 장기293개만 가시 상태 |
| 여성 전신 최초 진입·CT 상세 | 초기 계측에서는 각각 피부20개·위1개로 정상. 이후 반복의 복귀 경로에서 결함 재현 | 같은20개·1개 유지 |

가시 메쉬 수는 `traverseVisible`로 센 **장면의 표시 후보**입니다. 카메라 밖 메쉬는 실제 그리기에서 제외되므로1,220개가 모두 화면 픽셀로 보였다고 표현하지 않습니다. 그리기 호출 수도 투명 양면의 두 번 그리기 등으로 메쉬 개수와 다릅니다. 남성 골격 첫 프레임의1.0→0.05 재질 차이는 실제 렌더러 입력에서 직접 확인했습니다.

[수정 전 전체 계측 gzip](first-frame-before.json.gz), [수정 후 전체 계측 gzip](first-frame-after.json.gz). 스크립트는 `scripts/audit-first-frame.mjs`이며 출력은 `.cache/first-frame/audit.json`입니다. `maxSubmissionMs`는 자바스크립트 렌더 호출의 제출 시간이지 GPU 완료 시간이나 사용자 입력 지연의 전부가 아닙니다. 소수 실행의 시간 차이를 성능 개선율로 일반화하지 않습니다.

## 원인과 변경

새 그룹과 재질은 기본적으로 가시·불투명 상태였고, 요청한 박리·선택·계통·절개 설정은 비동기 수동 효과(`useEffect`)에서 적용했습니다. 데이터 로드·프레임 예약 순서에 따라 미설정 장면이 한 번 렌더러에 전달될 수 있었습니다.

네 표시 경로(기본 GLB, 전신 혈관/신경, 림프 참조, packed 여성/독립 상세)는 새 그룹을 숨긴 상태로 만들고 기존 가시성·재질 규칙을 `useLayoutEffect`에서 적용한 뒤 그룹을 드러냅니다. 준비 완료 통지는 기존 수동 효과에 유지해 설정보다 앞서지 않도록 합니다. 보기 상태 액션, 모델 좌표, 수록 ID, 박리 불투명도 공식, 선택 정책은 변경하지 않습니다.

이미 표시 중이던 이전 장면이 UI 입력과 새 3D 상태의 커밋 사이에 한 프레임 남는 현상까지 제거한 변경은 아닙니다. 독립 출처의 카메라 맞춤은 기존 준비 완료 이후 경로이며, 이 수정은 **새 장면의 미설정 재질/가시성 노출**을 차단합니다.

## 검증 범위와 남은 작업

단위61/61·TypeScript·프로덕션 빌드 통과. 새 브라우저 검사는 남성 골격·혈관·림프·신경의 첫 표시 불투명도, 여성/CT 신규 장면과 반복 복귀의 모든 관찰 프레임, 취소된 다운로드의 뒤늦은 노출을 대상으로 합니다. 관찰기는 초기 남성 피부 로딩 후 설치하므로 최초 페이지 로드 자체의 첫 프레임을 측정한 것은 아닙니다. 후속 실행 결과는 아래에 기록합니다.

이 결함이 이전 모든120초 이상의 로딩/응답 시간 초과를 설명한다고 입증하지 않았습니다. 해부 좌표를 고친 것도 아니므로 여성 보완 골격·근육의 남은 위치 불일치, 장기/신경의 세부 정합, 전체 근육 층 순서 검수는 계속 필요합니다.

첫 프레임2개·기존 로딩1개·출처 복귀2개를 한 실행에서5/5(1.2분), 추가 취소1개·여성 전체1,220개 좌표/박리/선택/복원1개·남녀 실제 근육 순서2개를 다음 실행에서4/4(47.1초) 통과했습니다. 여성/CT 청크를 의도적으로 지연한 뒤 원래 출처로 돌아가면, 뒤늦게 완료된 요청의 모형이 표시되지 않는 것도 확인했습니다. 전체39개 브라우저 검사를 반복한 것은 아닙니다.

직접 검토한 복귀 후 [데스크톱](first-frame-female-return-desktop.png)·[모바일](first-frame-female-return-mobile.png)에서 여성 장기 장면과 조작부를 확인했습니다. 이 정지 화면은 복귀 후 상태이며 최초 프레임 자체의 픽셀 캡처가 아닙니다.

프로덕션 검사도 남성9 GLB·여성15청크·요청 시 CT1청크·성별 전환·위키 비교 복원·브라우저 오류0으로 통과했습니다. 이 한 번의 통과가 이전 간헐적 지연 전체의 해결을 입증하지는 않습니다.

## 공개 반영

구현 `72f803faf64dfa52e10e465f237767b822d6c69c`를 [Pages 작업35567423121](https://github.com/jay-jang/gyeol-atlas/actions/runs/35567423121)으로 배포했습니다. 공개 여성 전신→CT→복귀→CT→복귀5단계에서 준비 완료 뒤의 정확한 가시 ID가20/1/293/1/293개로 일치했고 브라우저/HTTP 오류0입니다([공개 측정](pages-source-ready-verification.json)). 공개 검사는 준비 완료 후 상태이고, 최초 프레임 계측은 로컬의 앞선 증거와 구분합니다.

## agy 제한 검토

`c8fb6df2-acd6-4ed2-83b7-28e9be83ac3c`은 변경 코드와 첫 프레임 검사에 대한 읽기 전용 검토입니다. 임상 검증·브라우저 실행은 하지 않았습니다.

- 준비 완료가 레이아웃 설정 뒤의 수동 효과에 남는 점: 설정 전 완료 통지보다 안전한 순서입니다. agy의 ‘정확히 한 프레임 뒤’라는 표현은 스케줄러가 보장하는 고정 시간으로 받아들이지 않습니다.
- 자원 해제는 기존 수동 효과 정리 단계: 메쉬는 먼저 장면에서 제거되며, 이번 변경으로 해제 오류가 발생했다는 구체적 증거는 없습니다. 원본 GLB 공유 geometry를 임의로 해제하지 않습니다.
- 관찰기를 초기 피부 로딩 후 설치하므로 첫 페이지 로드 미포함: 맞는 범위 제한이며 명시했습니다.
- 진행 중인 다운로드 취소·빠른 출처 변경 미검사: 지적을 반영해 여성/CT 청크를 보류하고 출처를 되돌린 뒤 해제하는 검사를 추가했습니다.
