# 사용자 제공 여성 STL 원본 대조 — 2026-09-21

사용자가 Downloads에 받은 `Final 3D STL Models-stl.zip`을 확인했습니다. ZIP 무결성 검사를 통과했고 원본은 이동·수정하지 않았습니다. 새 작업 캐시에 추출했습니다. SHA-256: `9886eda040f6087bbb65182530f8bd262be456f366b155025c12b3dc56da774e`.

[대학 공식 여성 자료](https://digitalcommons.du.edu/visiblehuman/1/) 및 ZIP의 README가 CC BY4.0을 명시합니다. 이 ZIP은 여성 근육76·뼈28·연골16·인대8, 합계128개 STL입니다. README는 남녀 연구의 공통 설명이지만 실제 이 묶음은 모두 `VHF` 파일입니다. 원본 파일별 해시·삼각형 수·경계와 재현 결과는 [원시 기록](donor-source-comparison.json)에 있습니다. 상체 근육이나 전신 말초신경 자료가 새로 확보된 것은 아닙니다.

## 원본과 가져오기 변환 분리

고정된 상위 가져오기 코드의 축 후보24개, 공유 뼈 중심 대응,6개 묶음의 균일 배율·이동, 가장 가까운 뼈 상자 중심에 따른 묶음 배정을 재현했습니다.76개 근육 모두 기존 묶음과 일치하며, 단순화 전 manifest 경계와 최대 잔차는0.0000291mm입니다. 이는 모든 현재 단순화 정점의 동일성이나 실제 해부 정합 승인이 아닙니다.

같은 STL에 변환만 적용해 단순화 영향을 분리한 결과입니다. 값은 정점→상대 표면 최솟값이며 건 부착 길이나 연속 삼각형 간 정확한 최단거리가 아닙니다.

| 관계 | 원본 | 서로 다른 기존 변환 후, 단순화 전 |
| --- | ---: | ---: |
| 왼쪽 안쪽→바깥쪽 장딴지근 | 0.04999mm | 33.17252mm |
| 오른쪽 안쪽→바깥쪽 장딴지근 | 0.05999mm | 31.49649mm |
| 왼쪽 안쪽 장딴지근→가자미근 | 0.04996mm | 28.31533mm |
| 오른쪽 안쪽 장딴지근→가자미근 | 0.05006mm | 32.43813mm |

같은 `shank` 변환을 받는 바깥쪽 장딴지근–가자미근은 원본 약0.05mm 간격이 배율에 맞춰 유지됩니다. 위 네 관계는 **서로 다른 변환이 수십mm 간격을 만든다는 원인**을 이제 같은 정점·삼각형에서 재현했습니다. 이전 감사에서 원본 미확보 때문에 보류했던 이 제한적 인과만 업데이트합니다. 원본에 간격이0이거나 건으로 연결돼 있었다는 뜻은 아닙니다. 대학은 최종 모형의 겹침을 제거할 때0.05mm 간격을 사용했다고 설명합니다.

## 아직 적용하지 않은 것

안쪽 장딴지근은 `thigh`, 바깥쪽·가자미근은 `shank`로 배치돼 있습니다. 안쪽을 `shank`로 통일하면 원본의 근육 간 관계는 회복할 수 있지만, 무릎·대퇴골과의 관계가 좋아지는지는 별도 검증해야 합니다. **이번에는 런타임 좌표를 바꾸지 않았습니다.** 후보는 피부 이탈·골격 및 다른 근육의 새 교차·무릎 양쪽 부착 관계를 함께 비교해야 합니다. 피부 안에 넣는 것만으로 승인하지 않습니다.

재현:

```sh
npm exec --yes --package=node@24.14.0 -- node scripts/audit-donor-source.mjs \
  '추출한/Final 3D STL Models-stl' '다운로드한/Final 3D STL Models-stl.zip'
```

출력은 `.cache/donor-muscle/source-comparison.json`이며 앱에 변환을 쓰는 경로가 없습니다. 다운로드 접근 제한은 사용자 제공 파일로 해소됐습니다. 전체 목표와 실제 위치 교정은 계속 진행 중입니다.

agy가 원본 비교 스크립트·상위 코드·출력의 제한 범위를 읽기 전용 검토했습니다. 양의 간격을 실제 연결로 부르거나 변환 결과를 기계적 파열로 일반화한 최초 표현은 정정 요청했으며, [검토 범위와 정정](NEURAL_BONE_RELATIONS.md#agy와-화면-확인)을 구분해 기록합니다.

후속으로 [세 실제 정합 후보](CALF_REGISTRATION_CANDIDATES.md)를 계산했습니다. 근육 간 간격이나 뼈 잔차 개선과 별개로 새 피부 이탈·조직 교차가 남아 세 후보 모두 런타임 적용을 보류했습니다.
