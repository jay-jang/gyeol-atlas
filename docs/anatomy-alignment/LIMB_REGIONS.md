# 팔다리 골격 부위 분류 수정 — 2026-09-21

엄지 부분 교정 후 공개 화면에서 확인된 ‘엄지 선택→배’, ‘위팔뼈 선택→가슴’ 문제의 원인은 두 가지였습니다. 여성 카탈로그 생성기는 높이만으로 부위를 정했고, 남녀 렌더러도 별도의 높이/가로 범위 검사로 가시성을 판단했습니다. 팔을 내려놓은 자세의 손은 배·골반·다리 범위와 겹칩니다. 이는 앞선 자세 교정과 별개의 **부위 분류 오류**입니다.

`src/anatomy-region.ts`는 현재 원문명으로 식별한 골격 레이어의 팔다리 구조에만 편집 분류를 적용합니다. 손가락·발가락·손허리·발허리·손목·발목·긴뼈와 어깨띠 이름을 대상으로 하며, 신경·혈관·근육에는 이 단어 규칙을 적용하지 않습니다. 어깨띠는 이 UI의 ‘팔·손’ 범위에 포함합니다. 해부 조직 분할이나 모든 계통의 임상적 부위 지도가 아닙니다.

| 요구 | 직접 증거 | 범위/남은 한계 |
| --- | --- | --- |
| 엄지·위팔뼈 검색 시 올바른 부위 | 여성 카탈로그64개를 팔·손으로 변경. 검색과 3D 클릭은 같은 카탈로그와 기존 `view-state` 선택 액션 사용 | 과거 저장된 잘못된 부위 값의 일괄 마이그레이션은 하지 않음. 재선택 시 교정 |
| 손뼈가 배·골반·다리 필터에 섞이지 않음 | 여성 팔·손·어깨64개를5개 합성 높이×9부위에서 검사. 발56개도9부위 대조 | 선택 구조는 기존 정책대로 가시성 우선. 단독 모형을 잘라내는 필터가 아니라 메쉬 단위 표시 |
| 남녀 일관성 | 남성 분류 대상 팔66·다리66, 여성 팔64·다리74개. 같은 원문명 짝과 남성 손발가락 마디뼈56개 단위 검사 | 여성 무명 관절면/연골 등 일부 구조는 기존 높이 기반 추정 유지. 수록 개수나 위치 정확도를 뜻하지 않음 |
| 다른 계통·좌표 보존 | 골격 외 단어 오분류 방지 단위 검사. 공유 함수에서 기존 비대상 기하 필터 유지 | 전신 혈관·신경처럼 여러 부위를 지나는 구조의 부위별 분할은 미완료 |
| 기존 상태 경로 유지 | 기존 선택 액션의 부위/레이어 변경, 부위 변경 시 선택 초기화 검사 | 선택 확대는 기존 실제 경계 기반. 팔다리 필터의 카메라는 가시 구조 경계와 화면 폭에 맞춤 |

## 검증

단위61/61 및 TypeScript·프로덕션 빌드 통과. 첫 실제 브라우저2/2에서 남녀 검색·8부위 전환·박리·복원·대상 좌표 해시 불변을 확인했습니다. 그러나 직접 모바일 화면을 검토해 손끝이 잘리는 기존 고정 거리 카메라 문제를 발견했습니다. 따라서 팔다리 부위 선택과 해당 부위에서의 화면 크기 변경은 가시 구조 경계를 화면에 맞추도록 보완하고, 모바일에서 실제 경계8꼭짓점의 투영 좌표가 화면 안인지 검사합니다. 최종 재검증 결과는 아래에 별도로 기록합니다.

새 분류는 원본 모형·삼각형·정점·엄지 변환 계수를 변경하지 않습니다. 이 수정은 [엄지 부분 교정](THUMB_REFINEMENT.md)을 대체하지 않습니다. 다른 손·발가락 자세, 보완 근육의 이탈, 여성 고유 기관의 세부 해부 위치와 신경 주행, 근육 표층/심층의 전체 검수는 여전히 미완료입니다.

### 전체 회귀에서 발견한 수정 및 실패

최초 전체 브라우저 실행은 **30/36 통과**였습니다. 새 남녀 팔다리2개는 통과했지만 이를 전체 성공으로 보고하지 않습니다.

- 기존 부위 검사가 팔다리의 고정 카메라 높이1.1m/0.43m를 계속 기대해 실패했습니다. 이제 두 부위에서는 실제 가시 메쉬 경계 중심과 카메라 타깃의 오차를 검사하며, 나머지6부위의 기존 높이 검사는 유지합니다.
- 여성 가자미근을 선택한 뒤 근육 계통 전환 시 카메라가 움직이는 **실제 신규 회귀**가 있었습니다. 선택 카드가 사라지는 레이아웃 변경을 화면 크기 변경으로 잘못 취급했습니다. 팔다리 자동 재맞춤 조건을 실제 캔버스 폭/높이 변경으로 좁혔고, 기존 근육 검사에서 시점 보존을 다시 확인합니다.
- 나머지는7계통 캔버스 캡처 시간 초과, 여성 뇌 검사의 초기 페이지 이동 시간 초과, 여성 CT 시점 초기화 뒤 카드 간격 검사 시간 초과, 남성 근육 깊이 상태 확인 시간 초과입니다. 소프트웨어 WebGL 환경의 지연이 관찰되었지만 이 네 실패의 원인을 모두 GPU로 단정하지 않습니다. 해당 검사 내용을 제거하거나 허용치를 늘리지 않고 별도 재실행합니다.

실패 문맥은 `.cache/limb-region/full-suite-failures`에 보존했습니다. 그 뒤 코드의 카메라 조건을 수정하고 표적8개를 다시 실행하므로, 통과하더라도 단일 전체36/36 실행과 구분합니다.

### 후속 결과

카메라 조건 수정 후 표적8개 중7개가 통과했습니다(6.6분). 여성 근육 선택 후 계통 전환 시 카메라 보존, 남녀 모바일 팔다리 경계 투영, 선택 없는 수동 시점의 재로딩 복원도 통과했습니다. 남성 근육 깊이 상태 확인에서만 다시 시간 초과됐습니다.

남성 근육 검사를 단독으로 진단 로그와 함께 실행해1/1(1.1분) 통과했습니다. 20~64%의 기존 검사 입력 모두 저장 상태와 일치했고, 같은 실제 메쉬 경계·투명도·순서·선택 복원 검사를 유지했습니다. 검사 완화나 GPU 렌더링 중지는 하지 않았습니다. 고유36개 시나리오를 확인했으나, 최초 전체 실행30/36과 후속7/8·1/1을 합친 결과이며 단일 전체36/36으로 표현하지 않습니다. 간헐적 지연의 근본 원인은 여전히 미확정입니다.

직접 검토한 화면: [여성 데스크톱](female-limb-region-desktop.png)·[모바일](female-limb-region-mobile.png), [남성 데스크톱](male-limb-region-desktop.png)·[모바일](male-limb-region-mobile.png). 모바일에서는 관찰 영역을 오른쪽 이동/확대 도구와 분리해 양손이 캔버스 안에 들어옵니다. 좁은 화면에서 양팔을 함께 보려면 작아지므로 세부 관찰은 개별 선택·확대를 사용합니다.

배포 검사 스크립트를 로컬에서 선행 실행해 남녀8부위씩과 검색8개(총24확인)를 통과했습니다. 새 대상270개 실제 가시 ID만 대조하며 몸통/연결 조직 전체를 포함하지 않습니다. 첫 프로덕션 스모크는 CT→여성 전신 복귀 후 남성 버튼 클릭의 안정화 대기에서30초를 초과했습니다. 검사 내용을 바꾸지 않고 다시 실행하며 결과를 별도로 남깁니다. 전신201단계 좌표 감사는 이번 부위 수정에서 다시 실행하지 않았습니다.

두 번째 프로덕션 실행은 앞선 성별 전환을 통과했으나 위키에서 KI3 비교 화면으로 돌아올 때 모델 준비 대기가60초를 초과했습니다. 한 경로의 반복된 결정적 오류로 단정할 증거는 없으며, 로딩/응답 지연은 미해결로 남깁니다.

세 번째 동일 프로덕션 검사는 남성9 GLB·여성15청크·별도 CT1청크·성별 전환·위키 비교 복원·브라우저 오류0으로 통과했습니다. 최종 단위61/61·TypeScript·빌드도 통과했습니다. 공개 배포 결과는 후속 기록으로 구분합니다.

## 공개 배포 확인

구현 `9f393ddcaf6e86abaa75d7b509ff8ac5f866edae`는 [Pages 작업35563156664](https://github.com/jay-jang/gyeol-atlas/actions/runs/35563156664)으로 배포했습니다. [공개 페이지](https://jay-jang.github.io/gyeol-atlas/)에서 남녀8부위씩의 대상270개 가시 ID 대조와 엄지 양측·위팔뼈·엄지발가락8개 검색을 수행해 총24확인 모두 통과했습니다. 브라우저/HTTP 오류0입니다([측정 기록](pages-limb-region-verification.json), [여성 모바일](pages-female-limb-region-mobile.png), [남성 모바일](pages-male-limb-region-mobile.png)).

공개 확인은 이 범위의 선택·가시성·모바일 화면 검사이지 전신 임상적 위치 검수가 아닙니다. 공개 기존 전체 Pages 스모크와201설정 좌표 검사는 이번에 반복하지 않았습니다. 앞선 로컬 시간 초과도 이 공개 통과로 해결됐다고 주장하지 않습니다.

## agy 제한 검토

`a824a6cf-f006-45c8-bed9-227e5a8f9856`은 새 부위 함수·연결 코드·검사에 대한 읽기 전용 정적 검토입니다. 브라우저나 임상 검수는 하지 않았고, 후속 카메라 수정 전의 코드입니다.

- 비대상 구조의 여성 높이 기반 태그와 남성 미지정 태그 불일치: 기존 제한이며 이번 팔다리 골격 변경으로 해결됐다고 주장하지 않습니다.
- 전신 보완 렌더러의 ID 인자 생략: 실제로는 생성 시 `item.name = id`로 바꾸며 함수 기본값이 `mesh.name`을 받습니다. 현재 오동작이라는 증거가 아닙니다.
- 남성 림프가 별도 원본 태그 필터를 쓰는 점: 기존 출처 분류를 보존한 경로이며 새 골격 이름 규칙을 림프에 적용하지 않습니다. 모든 계통의 통합 부위 검수가 남습니다.
- 검사에서 몸통뼈→팔다리 누출을 모두 판정하지 않음: 맞는 한계입니다. 새 대상270개만 검증하며, 골반·갈비·척추 및 전체 연결 조직의 기하 기반 필터를 검증 완료로 일반화하지 않습니다.
- 골격 레이어의 반월판·슬개인대 같은 연결 조직이 이름 규칙에 포함되지 않음: 기존 기하 추정 유지이며 범위를 명시합니다.
