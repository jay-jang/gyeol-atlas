# 2026-09-20 배포 후 agy 재검토

대화 ID: `138e0b47-e481-43a7-80d4-6b8d1bd66df2`. `agy --mode plan --output-format json`으로 `40bdda4`와 후속 작업 트리를 읽기 전용 검토했습니다. 아래는 응답의 요약과 주 에이전트의 대조 결과이며, 원문 전체를 보존한 파일은 아닙니다.

agy는 소스·데이터와 단위 검사38개를 검토했습니다. agy가 브라우저를 실행하거나 임상 해부학 위치를 검증한 것은 아닙니다. 실제 브라우저 회귀는 별도 Playwright 실행입니다. 최초 응답의 여성 뇌75·심장10·자궁1이라는 개수는 잘못됐으며 실제 카탈로그의 뇌283·심장14·자궁10으로 대조하고 후속 응답에서 정정했습니다. 검토의 ‘PASS’를 전체 해부학 수록 또는 임상 정합 완료로 해석하지 않습니다.

| 검토 의견 | 직접 확인과 처리 |
| --- | --- |
| P1: 독립 상세에서 박리·계통 전환 시 확대 카메라 잔류 | 실제 남성 심장→박리0% 검사에서 전신 경계의 화면 정규화 좌표가14.465로 화면 밖에 남는 실패 재현. 준비가 끝난 새 자료의 전신 경계에 카메라를 맞추도록 수정. 동일 자료 내 박리는 카메라 보존 |
| P2: 여성 모드 도움말의 남성 개수 | `stages.description`의 남성 고정 수치가 여성 레이어·도움말에도 노출됨을 소스와 UI에서 확인. 여성 전신 카탈로그의 계통별 개수와 수록 한계로 교체. 별도 CT 상세는 전신 개수에 더하지 않음 |
| P2: 비 CT 상세 하단이 카드에 가려질 가능성 | agy는 실제 화면 측정을 하지 않았음. 별도 투영 경계·데스크톱/모바일 측정이 남음. CT에 적용한 여백을 무조건 전체에 적용하지 않음 |
| P3: 전신 복귀 시 사용자의 회전 방향 유지 | 사용자 관찰 방향을 보존하는 의도된 동작. 방향을 강제로 정면으로 바꾸는 수정은 하지 않음. 극단적인 회전 상태의 화면 맞춤 검사는 추가 여지 있음 |
| P3: 여성 경혈 장부 비교에서 CT 위를 찾지 못함 | HRA 전신 비교와 CT 상세는 다른 신체라 ID를 합치지 않는 것이 의도된 동작. ‘여성 원본에 없음’이라는 안내의 별도 CT 발견성 개선은 남음 |

후속 agy 검토는 `framedSource` 준비 상태 가드와 `stageDescription` 변경을 재검토해 제한된 diff에서 추가 회귀를 지적하지 않았습니다. 동일 자료의 박리 시 카메라 유지, 새 자료의 선택 확대와 충돌 방지, 성별 전환 시 새 전신의 로딩 완료 후 맞춤을 확인했습니다. 이 판단 역시 정적 검토이며 전체 요구 완료 판정은 아닙니다.

## 기관 상세 가림의 실측·후속 검토

대화 `c55ce317-2b5a-4856-bf39-e089090c9ae2`에서 새 카메라 계산, 카드 크기 관찰, 최초 시점 복원, 가로 상세 레이아웃을 두 차례 읽기 전용 검토했습니다. agy는 단위39개·타입 검사를 실행했다고 보고했고, 브라우저 검사는 실행하지 않았음을 후속 응답에서 구분했습니다. 주 에이전트의 직접 단위 실행도39/39·타입 검사 통과였습니다.

검토는 제한된 diff에서 추가 결함을 지적하지 않았습니다. 다만 처음 제시한 ‘카드42vh 제한이 관찰 영역58%를 보장한다’는 설명은 하단 도구와 캔버스 실제 크기를 무시한 잘못된 추정입니다. 뒤의 가로 화면 예상 픽셀 값 역시 전체 창과 캔버스 좌표를 혼용했으므로 측정 결과로 사용하지 않습니다. 검토가 남긴 코드 외 파일명·검사 분류의 오류도 지적했습니다.

실제 근거는 `detail-layout.spec.ts`와 `detail-projection.ts`의 브라우저 측정 및 캡처입니다. 남성 심장 최초149.93px 가림, 첫 가로 개선안의2.96px 여백 실패를 각각 재현한 뒤 수정했습니다. 모든26개 기관 묶음의 카드 접기·펼치기, 대표3자료의 시점·세부 구조·복원·화면 크기 전환 검사는 별도 실행 결과로 기록합니다. 단순한 agy 승인이나 카메라 화면 맞춤을 해부학적 위치·전체 수록 완료의 근거로 삼지 않습니다.

## 일반 비교·여성 CT 안내의 제한 범위 재검토

대화 `b1e964a7-8703-4b35-baef-b000697f8dc9`는 `comparisonReference`, 일반 선택의 카드 회피, 시점 복원, 안내 스크롤과 관련 검사를 읽기 전용으로 검토했습니다. 추가 코드 결함을 지적하지 않았고 단위40/40·타입 검사 통과를 보고했습니다. 주 에이전트의 직접 실행에서도 단위40/40과 빌드를 통과했습니다.

agy는 브라우저를 실행하지 않았습니다. 따라서 응답의 ‘가림 방지’·‘조작 접근 가능’·‘스크롤이 제한됨’은 정적 판단이지 화면 측정 결과가 아닙니다. 실제 후속 모바일 캡처에서는 밝은 안내 배경에 밝은 글자가 상속되는 대비 문제를 주 에이전트가 발견했습니다. 신규 CT 안내를 기존 가독성 검사에 추가하고 공유 색상 토큰으로 보완합니다. 별도 CT 선택도 좌표 병합을 허용하는 것이 아니라 다른 장면으로 이동하는 동작입니다. 실제 비교 경계·ID·복원·모바일 안내 검사 결과는 [검증 기록](VERIFICATION.md)을 기준으로 합니다.

## 실제 피부 표면 감사 검토

대화 `758677c5-3f77-43d6-9b5f-fdc9ab308d5f`에서 포함 검사·좌표 적용을 읽기 전용으로 검토했습니다. agy의 남성 다중 피부 경계에서 홀짝 검사가 신체 이탈을 잘못 판정할 수 있다는 지적을 반영해 판정 불가 표시와 중첩 상자 단위 검사를 넣었습니다. 좌표 변환을 새로 적용하거나 정합이 완료됐다고 승인한 검토가 아닙니다.

응답의 ‘여성 고유 골격231개’는 오류이며 실제로는 skeletal65개+connective76개,141개입니다. 보완 골격은180개입니다. `VHF0005` 왼 장딴지근을 최대24.66mm로 적은 것도 잘못됐고 해당 최대값은 `VHF0043` 오른쪽입니다. 최초 표본 결과를 ‘100% 포함’이라고 일반화한 문구도 채택하지 않습니다. 주 에이전트가 모든 여성 참조 정점으로 확대한 검사에서는 표본에서 놓친 보완 근육1개가 추가로 발견됐고, 피부 관련 유방 모형을 포함해 유두·젖샘관의 별도 표면 이탈도 기록했습니다.

직접 검사 보고서와 공개 피부·팔뼈 캡처가 증거입니다. agy는 브라우저/의학 검증을 실행하지 않았습니다. 제안된 winding-number 방식도 중첩 피부에서 자동으로 올바른 신체 외피를 만드는 해결책이라고 가정하지 않습니다. [최종 범위와 미완료 항목](BODY_CONTAINMENT.md).
