import {chromium} from '@playwright/test';import fs from 'node:fs/promises';const b=await chromium.launch({headless:true,args:['--no-sandbox','--enable-unsafe-swiftshader']});try{const p=await b.newPage();await p.goto('http://127.0.0.1:3002/#atlas/CV12');await p.getByText('해부 모델 로드 완료').waitFor();await p.getByRole('button',{name:'대응 장부의 해부 구조 비교'}).click();await p.getByRole('link',{name:'마사지 전 해부학 참고 가이드 ↗'}).click();const guide=p.url();await p.getByRole('link',{name:'3D 경혈 지도',exact:true}).click();const returned=p.url();const result={guide,returned};await fs.writeFile('docs/ux-review-2026-09-10/wiki-return.json',JSON.stringify(result,null,2));console.log(result);}finally{await b.close();}
